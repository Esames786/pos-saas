# TEAM E — cross-cutting audit (READ-ONLY) — Edge HEAD 4affc67 vs installed 0.6.0-edge (623f887) vs Online

Verbatim hand-back of the Team E review agent, 20 Sep 2026. Legend: paths relative to `D:\laragon2\www\pos-saas-edge`; `INST` = `C:\Users\Dell\BingooEdgeLab\install\BingooEdge\runtime\versions\0.6.0-edge`. Edge views/routes/controllers/middleware/config are byte-identical between HEAD and INST (see E-13), so EDGE_INSTALLED_* = EDGE_SOURCE_* for every cashier-facing record below.

## (1) NAVIGATION COVERAGE

**E-01 Online POS page has NO app chrome; Edge POS has a header nav**
ONLINE_ROUTE=`tenant.pos.index` GET /pos · ONLINE_VIEW=`resources/views/layouts/app.blade.php:23-32,54-57` (`body.pos-workspace .header{display:none}`, `.nosidebar .sidebar{display:none}`), `partials/header.blade.php`, `partials/sidebar.blade.php` · EDGE_ROUTE=`edge.local.pos.screen` GET /edge/local/pos · EDGE_VIEW=`resources/views/edge/pos/index.blade.php:112-135`
ONLINE_BEHAVIOUR=On /pos the header (clock, POS button, fullscreen, user menu, Change Password, Logout `header.blade.php:49-138`) and the whole sidebar are hidden; the operator leaves the POS only by URL/browser. In-page nav = Report / Return / Quick Report / Add-Search Customer (`tenant/pos/index.blade.php:499-535`), Change branch/terminal (:578-582), "Open shift" link → `/shifts/open` (:570), Bill Preview / Request Bill on the session bar (:457-469), 19 Bootstrap modals (billPreview, changeOrder, completedOrders, customer, deadSession, heldSales, lastPrint, modifierEntry, payment, posContext, posReport, posReturn, printHere, qtyEntry, quickReport, reservationDetails, reserveTable, splitBill, tableWorkspace), Ctrl+F/H/L/P/Enter/M shortcuts (:6608-6615).
EDGE_BEHAVIOUR=Header: `POS`, View Tables, branch·user, Order select, Terminal select, Shift, Returns, Suppliers/Journal/Purchase Returns (permission-conditional :123-131), Status (:132), sync chip, Logout form (:134). Action grid (:341-366): Hold/Draft/Recall/Preview Bill/Review & Pay (+ Save round/KOT/Split Bill/Cancel order/Leave check on a held check) + Quick Report + Recent Prints. Finance pages carry POS/Suppliers/General Journal/Logout (`edge/finance/suppliers.blade.php:83-87`, `journal.blade.php:69-73`, `purchase-returns.blade.php:83-85`); health page Back to POS/Logout (`edge/health.blade.php:53-54`).
NAVIGATION_DIFFERENCE=Edge exposes Logout, Status, Suppliers, Journal, Purchase Returns on the till (Online reaches them via the hidden sidebar, never from POS). Online POS has no Logout/Status. Keyboard shortcuts absent on Edge (only Enter in Returns search :483). STATUS=FUNCTIONAL_BUT_UI_DIFFERENT · ACTUAL_BROWSER_PROOF=header items "Shift / Returns / Status / Synced / Logout" and actions "Hold / Draft / Recall / Preview Bill / Review & Pay / Quick Report / Recent Prints" seen on the cashier laptop (`docs/status/edge-p5c-home-lab.md:219`; `BingooEdgeLab/evidence/online-lan-baseline.json` cashier.pos_page) · AUTOMATED_TEST_PROOF=`tests/MySql/EdgeCashierScreenRendersHttpMySqlTest.php:94-97` (server HTML only) · REQUIRED_ACTION=owner decision whether Edge header should mirror Online's "no chrome" POS or Online should gain Logout/Status; record shortcut parity as gap.

**E-02 Online operator sidebar entries with no Edge equivalent**
ONLINE_VIEW=`partials/sidebar.blade.php` (operator-visible per `docs/plans/kashif-food-operator-permission-hide-2026-08-31.md:81-83,156-160`): Dashboard (:80), Operations → Branches :169 / Terminals :177 / Shifts :185 / Daily Closing :193 / Currencies :201; Sales → POS :476 / Sales Orders :484 / Sales Returns :492 / Sales Ledger :532 (Customers :500, Payment Methods :508, Delivery Channels :516, Riders :524 revoked for Kashif operators); Restaurant → Table Board :555 / Kitchen Display :564 / Floors :573 / Tables :581 / Waiters :589 / Held Sales :597; Kitchen Inventory :611-655; Printing → Printers :1328 / KOT Routing :1337 / Layouts :1345 / Print Jobs :1354 / Print Agents :1362; Sales Controls → Promotions/Combos/Service Charge/Void Reasons :1273-1301; Reports :1124-1258 (Sales Report Center :1133, Shift Reports :1160, Print Reports :1250 — revoked for Kashif operators); Administration → Users/Roles/Billing/System Reset :1417-1449; Change Password (`header.blade.php:127`).
EDGE_BEHAVIOUR=Edge route census is only `edge/local/pos/*` (`routes/edge_runtime.php:37-113`). Present as in-page equivalents: Table Board (View Tables modal :792-845), Held Sales (Recall modal :574-588), Shifts open/close for the selected terminal (Shift modal :925-962), Sales Returns (Returns modal :469-549), Print Jobs (Recent Prints :713-743), Supplier Payments/Manual Journal/Purchase Returns pages, Status/health.
NAVIGATION_DIFFERENCE=MISSING_IN_EDGE: Dashboard; Sales Orders list/detail (Online "Completed Orders" modal :1183 + `/sales-orders/{id}` links :5683-5684, `/api/pos/recent-sales`); Sales Ledger; Shifts list/show + Daily Closing + Close-Branch (`tenant.shifts.close-branch`); Terminals/Branches/Currencies; Customers page and quick-create (Edge :430 "Adding a new customer needs the Online POS"); Payment Methods; Delivery Channels/Riders mgmt; Kitchen Display; Floors/Tables/Waiters mgmt (Online Table Board offers management buttons :1035-1043); Printers/KOT Routing/Layouts/Print Agents; Void Reasons mgmt; Kitchen Inventory; Reports (incl. POS "Report" button = Sales Report Center window :499-510); Users/Roles/Change Password; Change Order Details modal (`held-sales/{id}/reattach-table`, `table-sessions/{id}/move|merge`) — table move already recorded in 4affc67; Request Bill (bill_requested) :461-469; Ctrl-key shortcuts and calculator (Ctrl+M). STATUS=MISSING_IN_EDGE (per entry; Sales Report Center/Reports may fall under the accepted "Cloud reports" set — owner to confirm; till-created customers already ACCEPTED_ONLINE_REQUIRED) · AUTOMATED_TEST_PROOF=none · REQUIRED_ACTION=owner to classify each entry as required-offline vs Cloud-only; add register rows.

## (2) FEATURE VISIBILITY BY ROLE / PERMISSION

**E-03 Online gating model vs Edge gating model**
ONLINE_CONTROLLER_OR_SERVICE=`app/Http/Middleware/EnsureRoutePermission.php:12-73` (`$user->can(routeName)` → 403 "Permission denied."; allow-prefixes :27-54 incl. `tenant.api.pos`, `tenant.pos.quick-report`, `tenant.restaurant.tables.reserve|unreserve|reservation`, `tenant.printing.documents|jobs|layouts.preview`, `tenant.api.server-time`) applied by `routes/tenant.php:170` together with `tenant.subscription.access` (plan entitlement; also `partials/header.blade.php:73-85`, `sidebar.blade.php:14-31`). EDGE=`routes/edge_runtime.php:37` middleware `edge.auth`+`edge.branch` only (`app/Http/Middleware/EnsureEdgeAuthenticated.php:33-67` session + fresh active/employee_code/branch-authorized user + active credential of the current epoch; `EnsureEdgeBranchBound.php:26-43`). No route-name permission middleware; permissions are per-controller. Permission source on Edge = bootstrap export of each user's EFFECTIVE set (`app/Services/Edge/EdgeBootstrapService.php:797-820`) imported as direct `model_has_permissions` (`EdgeLocalBootstrapImporter.php:369-415`). PERMISSION_PARITY=model differs (default-deny by route name vs default-allow + explicit checks). STATUS=FUNCTIONAL_BUT_UI_DIFFERENT (see E-04/E-05 for concrete gaps).

**E-04 Gates Edge enforces the same as Online (server-side)**
tenant.pos.store → Complete Sale button + server 403 (`EdgeLocalPosController.php:170,1115-1124`; Online `tenant/pos/index.blade.php:997`, route :476) — AUTOMATED_TEST_PROOF=`tests/MySql/EdgeCashierPermissionHttpMySqlTest.php:97-131`. tenant.sales-returns.store → `EdgeLocalPosController.php:979-984` (Online :507). tenant.pos.quick-report-send → `EdgeQuickReportController.php:38,49` + branch scope :71 (Online `PosQuickReportController.php:35,44`; Online view :522). Supplier finance `tenant.suppliers.ledger` / `tenant.supplier-payments.store` / `tenant.finance.manual-journals.store` (`EdgeLocalSupplierFinanceService.php:29-31`; controller :26-155; header links :123-128). Purchase returns `tenant.purchase-returns.store`+`.post` (`EdgeLocalPurchaseReturnService.php:32-33`; controller :55-56,92-93). Order-type authority `allowsOrderType` (`EdgeLocalPosService.php:125-129,606-607,692-696` → 422 ValidationException; Online `HeldSaleController.php:324,478,921`, `RestaurantTableSessionController.php:588-596` → 403) — VALIDATION_DIFFERENCE=status code/message differ. Hide-amounts `AmountVisibility` (`app/Support/AmountVisibility.php:26-33`) applied in Edge shift summary (`EdgeLocalPosController.php:379,418`; UI `*****` + blind-count note `edge/pos/index.blade.php:929,939`) as on Online Close Shift. Manager approvals require the manager's own permission (`EdgeLocalPosService.php:1034-1068`, `EdgeLocalAuthService.php:58-68`). STATUS=MATCHED_AND_PROVEN for tenant.pos.store (HTTP test); FUNCTIONAL_BUT_NOT_BROWSER_PROVEN for the rest.

**E-05 Online gates Edge does NOT enforce**
ONLINE: `tenant.pos.index` (page, route :475) · `tenant.held-sales.store` (Hold/Draft/Add round :568) · `tenant.held-sales.cancel` :569 · `tenant.restaurant.board` :534 · `tenant.restaurant.table-sessions.open` :535 (also the in-controller reservation gate `RestaurantTableController.php:16,20`) · `.close` :537 · `tenant.sales-orders.split-bill.store` :563 · `tenant.shifts.create/store/close` :241-245 plus terminal binding `UserDataScope::assertCanOperateTerminal` (`app/Services/Security/UserDataScope.php:178-192`; `ShiftController.php:148,269`) · `tenant.api.manager-approvals.verify` (route :710-712, NOT in allow-prefix → permission by name) · `tenant.pos.change-terminal` pin enforced server-side (`UserDataScope.php:118,134-144` 403 "You can only work on your own terminal.") · terminal/branch scope on print jobs (`PrintJobController.php:315-325`).
EDGE: `screen()` :65-184 has no `tenant.pos.index` check (any Edge-eligible user per `app/Support/EdgeUserAuthz.php:17-43` opens the till); `storeHeldSale` :604, `openTable` :566, `restaurantBoard` :528, `closeTableSession`, `splitHeldSale`, `reserveTable`, `openShift` :347-358, `closeShift` :435-446, `verifyManagerApproval`, `printJobs` :1030 — grep of `->can(`/`abort(403` over `EdgeLocalPosController.php`, `EdgeLocalPosService.php`, `EdgeTableReservationService.php` finds none for these; `selectTerminal` :310-322 accepts ANY active terminal of the branch (pin is UI-only :74-79,120,214); shift open/close on the session terminal with no `canOperateTerminal`. `cancelHeldSale` :1223-1241 delegates to canonical `KotCancellationService` ("permission + branch approval mode" per comment — internals not read). PERMISSION_PARITY=GAP · STATUS=PARTIALLY_IMPLEMENTED · AUTOMATED_TEST_PROOF=`EdgeCashierScreenRendersHttpMySqlTest.php:110-122` proves only that the pinned operator's HTML omits other terminals, not that POST /terminal/select refuses them · REQUIRED_ACTION=enforce `tenant.pos.index` at screen(), `tenant.pos.change-terminal` + terminal binding at `selectTerminal`/`openShift`/`closeShift`, and the held-sales/table-session/split/board/manager-verify permissions server-side; add HTTP tests.

## (3) TERMINAL / BRANCH RESTRICTIONS

**E-06** ONLINE_VIEW=`tenant/pos/index.blade.php:560-582,625-658` (context row + `posContextModal`: Branch select over all assigned branches, Terminal select with "No Terminal" option :647, Change button `@can(change-terminal)`); ONLINE_CONTROLLER=`POSController.php:423-429` (`terminalsForPos` filtered to default for pinned), `UserDataScope.php:44,65,82-115,134-144`; POS-DEFAULT-TERMINAL-1 JS :2460-2464. EDGE_VIEW=`edge/pos/index.blade.php:119-120,207-220` (single Terminal `<select>` in header; auto-select default else first; disabled only if `!canChangeTerminal && terminals.length<=1`); EDGE_CONTROLLER=`EdgeLocalPosController.php:72-79,274-322,1127-1142` (session key `edge_pos_terminal_id`; sale/shift require a selected terminal → 422 "Select a terminal first."). WORKFLOW_DIFFERENCE=No branch selector on Edge (bound branch, `EnsureEdgeBranchBound`); no "No Terminal" mode (Online allows selling without a terminal, auto-print off :572-574); Online Change is a modal, Edge an always-visible select. PERMISSION_PARITY=pin not enforced server-side (E-05). Note "POS-TERMINAL-PIN-1" means *pinned*, not a PIN code. STATUS=FUNCTIONAL_BUT_UI_DIFFERENT · AUTOMATED_TEST_PROOF=`EdgeCashierScreenRendersHttpMySqlTest.php:99,104,110-122` · ACTUAL_BROWSER_PROOF="Lab Counter 1" shown (`edge-p5c-home-lab.md:219`).

**E-07 Manager re-auth** ONLINE=`/api/manager-approvals/verify` manager PIN (`routes/tenant.php:708-712`, throttle 10/min). EDGE=manager employee code + own Edge credential (`edge/pos/index.blade.php:439-468`; `EdgeLocalPosService.php:1047-1068`; `EdgeLocalAuthService.php:58-68`, manager_pins never consulted). VISUAL/WORKFLOW_DIFFERENCE=different prompt fields; Edge modal replaces the current modal (no stacking) and re-renders it afterwards (:533-536). STATUS=FUNCTIONAL_BUT_UI_DIFFERENT.

## (4) EMPTY / LOADING / ERROR STATES, DIALOGS

**E-08** ONLINE_VIEW=`layouts/app.blade.php:62-64` global loader, :111,:115-124 SweetAlert2 flash toast; `tenant/pos/index.blade.php` Bootstrap modals (19), `Swal.fire` dialogs (:2694,3237,4006,4327,4478,4565,4621,4684…), `toast(icon,title)` :3784-3800, `spinner-border` loading (:1175,1204,1369,5007,5407,5668), empty states ("No products found." :2821, "No held orders for this branch." :5016, "No completed orders yet…" :5675), native `confirm()/alert()` (:1325,1335,4942,4959,5222,6161,6284), network-error toasts (:4727,4823,6576), `deadSessionModal` static-backdrop (:1217). EDGE_VIEW=`edge/pos/index.blade.php`: ONE generic overlay `#modal` with innerHTML swap (:75-79,166,202-204; click-outside closes :204; width min(720px,94vw)), single unstyled text toast 3.2 s (:87,200 — no severity/icon), inline `.err` boxes (:86), NO loading spinners anywhere, empty states ("Cart is empty." :157/291, "No products found." :261, "No open checks." :578, "Nothing printed yet." :717, "No floors configured." :804, "No returnable sale matches." :478), one native `confirm()` (:608). Error surface: `api()` :189-199 → network failure shows the offline banner (:140,194) and "The branch server did not answer…"; non-OK → `json.message` or flattened `errors` or "Request failed (status)". 401 from `edge.auth` on fetch → JSON `Unauthenticated.` (`EnsureEdgeAuthenticated.php:71-73`) shown as toast, no redirect to login (Online: `redirectGuestsTo('/login')` `bootstrap/app.php:65`, `EnsureRoutePermission.php:65-67` for page loads). 419 CSRF → generic toast, no recovery on either side. Page-load 403 → Laravel default error page on both (no `resources/views/errors`; only `tenant/errors/module-disabled`). Finance pages: red toast 6 s (`suppliers.blade.php:73,180-185`), stale/mode banners (:89-90), `api()` throws `HTTP <status>` (:188-203). Health page: server-rendered, meta auto-refresh 30 s, no JS (`EdgeLocalHealthController.php:14`; `health.blade.php:58`). Login: Online `resources/views/auth/login.blade.php` (layouts.auth, email+password+remember) vs Edge `edge/auth/login.blade.php` (employee_code + credential, generic "Invalid credentials." `EdgeLocalAuthService.php:82`). VISUAL_DIFFERENCE=Bootstrap/Swal light theme with icons & spinners vs dark, icon-less, spinner-less custom UI. STATUS=FUNCTIONAL_BUT_UI_DIFFERENT · ACTUAL_BROWSER_PROOF=login page + POS render only · REQUIRED_ACTION=add loading indicators, severity toasts, session-expiry redirect on 401/419.

## (5) RESPONSIVE LAYOUT / SCALING

**E-09** ONLINE=Bootstrap **v5.3.8** (`public/assets/css/bootstrap.min.css` header) + Tabler icons + FontAwesome + Nunito/Poppins; `tenant/pos/index.blade.php:7-12` `.pos-shell` grid `minmax(0,1fr) 500px`, tiles `minmax(170px,1fr)` (:126, 145px variant :377), cart sticky top 88px height `calc(100vh-104px)` (:261-268), action buttons min-height 42px (:286), `@media (max-width:1199px)` → single column, cart static (:342-346); `@media (min-width:1200px) and (min-height:720px)` → fixed shell `calc(100vh-188px)` with internal scroll (:348-360); modals `modal-xl modal-fullscreen-lg-down` (:1020,1141,1356), report/return windows `min(1680px,96vw)` (:1455); POS content padding 12px, min-height 100vh (`layouts/app.blade.php:29`). EDGE=`edge/pos/index.blade.php:25-93`: dark theme, `body{height:100vh;overflow:hidden;font-size:14px}` (:27), `main{grid-template-columns:1fr 380px}` (:45) at EVERY width — **no `@media` rule in any Edge view**; tiles `minmax(130px,1fr)`, min-height 74px (:50-51); header `flex-wrap` (:28); buttons ≈34 px tall (:35); modal `min(720px,94vw)`, max-height 88vh (:77); finance grids 360/380 px fixed (`suppliers:37`, `purchase-returns:38`, `journal:35`); health `auto-fill minmax(340px,1fr)` (:26).
VISUAL_DIFFERENCE (1366×768 laptop, viewport ≈ 650–690 px tall): Online fails the `min-height:720px` query → flowing page with page scroll and sticky cart; Edge is a fixed 100vh app, no page scroll, internal scroll only. Tablet (≤1199 px): Online stacks cart under products and goes fullscreen for large modals; Edge keeps two columns — at 768 px portrait the product pane is ~388 px (2–3 tile columns), the header wraps to 2–3 rows and `overflow:hidden` can clip, modals stay 94vw floating. Touch targets smaller on Edge (34 vs 42 px). STATUS=FUNCTIONAL_BUT_UI_DIFFERENT · ACTUAL_BROWSER_PROOF=none at those sizes (source reasoning only) · REQUIRED_ACTION=add ≤1199 px stacking + touch sizing to Edge, or accept.

## (6) ASSET LOADING WITHOUT INTERNET

**E-10** ONLINE_VIEW=`layouts/app.blade.php:9-22,104-112` — all CSS/JS via local `asset()` (theme-script, bootstrap, datetimepicker, animate, select2, daterangepicker, tabler-icons, fontawesome, style.css, a11y-custom; jquery-3.7.1, feather, slimscroll, bootstrap.bundle, moment, daterangepicker, select2, sweetalert2, script.js); no `@vite`, no `public/build`. **External:** `public/assets/css/style.css:3-4` `@import url("https://fonts.googleapis.com/css2?family=Nunito…")` and `…Poppins…` — the only Internet URLs (blocks font paint offline, then falls back). Header clock resync `/api/server-time` same-origin (`header.blade.php:53,212-223`). EDGE=zero `<link>`, `<script src>`, `asset()`, `@vite` or `http(s)://` in `resources/views/edge/**` (grep empty); inline CSS/JS only (`edge/pos/index.blade.php:10`). `INST/public/assets` ships the Online assets (identical to source except `img/supplier` only in source) but no Edge view references them. STATUS=MATCHED_AND_PROVEN (offline-safe by construction; rendered on the LAN cashier laptop `edge-p5c-home-lab.md:219,231`) · NOT_VERIFIED=`window.open(job.preview_url)` (:709) origin not traced.

## (7) TLS / SESSION BEHAVIOUR

**E-11** config: `config/session.php:21` driver `env(SESSION_DRIVER,'database')`, :35 lifetime 120, :50 encrypt false, :130-133 cookie `slug(APP_NAME)-session`, :172 secure `env(SESSION_SECURE_COOKIE)`, :185 http_only true, :202 same_site lax. EDGE overrides: `scripts/edge/appliance/appliance.env.template:15-16` `SESSION_DRIVER=file`, `SESSION_SECURE_COOKIE=true`; installed `appliance.env` carries keys `SESSION_DRIVER`, `SESSION_SECURE_COOKIE` (names only), no `SESSION_LIFETIME` → 120 min idle; evidence `health-cashier-phase.json` runtime.session_driver=`file`. `config/app.php:140` app.key from `EDGE_LOCAL_APP_KEY` on branch_server. `bootstrap/app.php:66-72` `trustProxies` loopback only when `APP_ROLE=branch_server` (nginx TLS gateway → HTTPS honoured); CSRF exempt only `api/edge/*` + print-agent (:57-63) — Edge POS fetches send `X-CSRF-TOKEN` (`edge/pos/index.blade.php:22,190`). Login: Edge `throttle:edge-login` 10/min per employee_code+IP (`routes/edge_runtime.php:29`; `app/Providers/AppServiceProvider.php:49-53`) + per-credential DB lockout 5 attempts/300 s (`EdgeLocalAuthService.php:25-26,105-115`), `session()->regenerate()` on login, `invalidate()+regenerateToken()` on logout (`EdgeLocalAuthController.php:48,53-61`), per-request freshness re-check that logs out stale sessions (`EnsureEdgeAuthenticated.php:42-64`). Online: `app/Http/Controllers/Auth/AuthController.php:14-17,34-55` RateLimiter email+IP+guard (no DB lockout), :127-128 logout; no route throttle on `tenant.login.post` (`routes/tenant.php:116`). AUTOMATED_TEST_PROOF=`tests/Feature/Edge/EdgeLocalAuthHttpTest.php:41` (throttle), :52-62 (Secure/HttpOnly/SameSite=lax cookie), :63 (ready reports secure_cookie/session_local). ACTUAL_BROWSER_PROOF=valid TLS padlock + login + `/edge/local/status` from the cashier laptop (`edge-p5c-home-lab.md:218-231`; `online-lan-baseline.json` cashier.https_valid=true, login authenticated). STATUS=MATCHED_AND_PROVEN (Edge stricter than Online) · NOT_VERIFIED=production Online `.env` session values.

## (8) CORRECT LOCAL ROUTE / API USAGE

**E-12** EDGE_VIEW=`edge/pos/index.blade.php:174` `BASE=url('/edge/local/pos')`; every `api()` path is relative to BASE and resolves to an `edge.local.pos.*` route in `routes/edge_runtime.php:37-113`: /terminal/select(:41) /preview-bill(:50) /customers(:90) /manager-approvals/verify(:97) /returns/search(:53) /returns/sales/{id}(:54) /returns(:55) /held-sales(:86,91) /held-sales/{id}(:87) …/kot(:92) …/settle(:93) …/cancel(:94) …/split(:96) /sales(:48) /sales/{id}/receipt(:100) …/kot-reprint(:101) /print-jobs(:102) …/printed(:104) …/retry(:105) /void-reasons(:88) /restaurant/board(:78) /restaurant/tables/{id}/open|reserve|unreserve(:79,82,83) /restaurant/table-sessions/{id}/close(:84) /quick-report/options|view|network|email(:108-111) /shift/summary|open|close(:44,46,47) /sync/summary(:45). Header hrefs → /edge/local/pos/suppliers, /finance/journal, /purchase-returns, /health, POST /edge/local/logout (:124-134). Finance pages use `BASE+path` (`suppliers:188`, `journal:129`, `purchase-returns:161`) and local hrefs; health local. No tenant route, no absolute host (`https?://` grep empty). Unused by the page but registered: GET /terminals, GET /shift, GET /returns/{return}, GET /finance/events/{event}. Cloud-only links that would fail offline: none. STATUS=MATCHED_AND_PROVEN for route targeting (census `EdgeBranchServerRegistrationTest.php:88-157` + page render); individual calls FUNCTIONAL_BUT_NOT_BROWSER_PROVEN (lab did not punch/pay).

## (9) EDGE SOURCE vs INSTALLED ARTIFACT

**E-13** `git diff --stat 623f887..HEAD -- app routes resources config` = 6 files: `app/Exceptions/EdgeAuthorityRefusedException.php` (+23), `app/Exceptions/EdgeStaleHeartbeatException.php` (+17), `app/Http/Controllers/Edge/EdgeAuthorityApiController.php` (+4), `app/Services/Edge/EdgeAuthorityLeaseClient.php` (+3/-1), `EdgeAuthorityLeaseService.php` (+15/-2), `EdgeAuthorityService.php` (+17/-3). Whole diff 14 files: + docs (`edge-cashier-architecture-clarification-2026-09-20.md`, parity register, p5-physical-certification, p5b-release-operations, p5c-home-lab) + tests (`EdgeAuthorityLeaseHttpMySqlTest`, `EdgeCleanMachineInstallMySqlTest`, `EdgeConnectionStateMachineMySqlTest`). `diff -rq` source↔INST: **app/** → exactly 3 files differ (`EdgeAuthorityLeaseClient`, `EdgeAuthorityLeaseService`, `EdgeAuthorityService`); 124 "only in source" = Cloud-only build exclusions (Catering, Manufacturing, Central, Cloud-side Edge API controllers, ingestion/projection services, `EdgeStandbyAdvertiser`, `EdgeRecoveryKeyCommand`, and the 2 new exceptions); **routes/**, **resources/views/edge/**, **config/** (incl. `edge.php`, `session.php`), **bootstrap/app.php**, `EdgeLocalPosController`, `EnsureEdgeAuthenticated` → IDENTICAL; `public/assets` identical except `img/supplier` only in source. INST manifest: `edge-build-manifest.json` edge_app_version 0.6.0-edge, artifact_commit 623f887 (`health-cashier-phase.json` runtime.artifact_commit).
EDGE_BEHAVIOUR (installed) differs ONLY in the heartbeat lost-ACK path: installed `EdgeAuthorityLeaseClient` throws `RuntimeException` (no `EdgeAuthorityRefusedException`), so installed `EdgeAuthorityService::heartbeat()` cannot adopt the Cloud `seq` on `STALE_HEARTBEAT` → after one lost ACK the appliance re-sends the same seq forever and counts CONNECTION_LOST (evidence: `health-cashier-phase.json` consecutive_failures 58, connection_state preparing_local; after the fix `online-lan-baseline.json` consecutive_acks 61, connection_state online). Cloud-side half (`EdgeAuthorityLeaseService` idempotent same-seq re-ack + 409 `seq`, `EdgeAuthorityApiController`) is not part of the appliance artifact. No cashier-facing (view/route/controller/middleware) difference. STATUS=PRESENT_IN_SOURCE_NOT_INSTALLED (heartbeat resync); cashier UI = identical to HEAD · REQUIRED_ACTION=cut a new release artifact from HEAD before pilot; NOT_VERIFIED=whether the LAB Cloud process runs HEAD.

## (10) WHAT THE TWO TESTS PROVE ABOUT THE SCREEN

**E-14 `tests/Feature/Edge/EdgeBranchServerRegistrationTest.php`** (:40-175) — boots as branch_server; asserts `Route::has` for health/ready/build-info (:45-47), Cloud routes absent (:50-62), health/build-info 200 + ready 503 payload (:65-81), EXACT URI census of the approved surface incl. `edge/local/pos`, `/pos/health`, quick-report, shift/sync summary (:88-167), 404 for `/pos`, `/login`, `/api/edge/pair` (:169-175). It never renders the cashier page: **no control is asserted**. STATUS=MATCHED_AND_PROVEN for route surface only.

**E-15 `tests/MySql/EdgeCashierScreenRendersHttpMySqlTest.php`** (:87-130) — real HTTP GET `/edge/local/pos` as branch_server, actingAs cashier; asserts server HTML contains `<h1>POS</h1>`, `View Tables`, `Review &amp; Pay`, `Preview Bill` (:94-97), terminal `Counter One`, product `Chicken Tikka`, deal `Family Deal` (:99-101), `edge-pos-data`, `"defaultTerminalId":N`, `"operationalStockReady":true` (:103-105); pinned operator → `Counter Two` absent, `"canChangeTerminal":false` (:110-122); unauthenticated → 302 `/edge/local/login` (:125-130). NOT asserted: Shift/Returns/Status/Logout buttons, Suppliers/Journal/Purchase Returns links, order-type select, category tabs, `canCompleteSale`, offline banner, any modal, and every JS-rendered control (Hold/Draft/Recall/Quick Report/Recent Prints/tiles) — they do not exist in server HTML, so this test cannot see them (no JS execution). Note "Review &amp; Pay"/"Preview Bill" match only the Blade header comment/JS source text, not a rendered button. STATUS=FUNCTIONAL_BUT_NOT_BROWSER_PROVEN (render + view-model only). Supporting: `EdgeCashierPermissionHttpMySqlTest.php:97-131` (403 without tenant.pos.store), `EdgeLocalPosRouteCensusTest.php:51` (pos routes auth-gated), `EdgeLocalAuthHttpTest.php:35-75`.

## Browser proof available (all records)
Only: LAB CA trust + valid TLS, `/edge/local/login` render, login `LAB2C5D`, `/edge/local/status`, `/edge/local/pos` render with header/actions/3 products/Synced badge on a second Windows 10 laptop over LAN (`docs/status/edge-p5c-home-lab.md:161,214,218-231,241,256`; `evidence/online-lan-baseline.json` cashier block). "Nothing punched or paid" — no sale, hold, KOT, return, shift, print, finance page, health page or error state was browser-exercised.

## Could NOT verify (and why)
1. Runtime layout at 1366×768 / tablet — read-only, no browser run; CSS reasoning only (E-09).
2. Production Online `.env` session/TLS values — no access (E-11).
3. Whether the LAB Cloud process runs HEAD (Cloud half of the heartbeat fix) — outside the artifact (E-13). [Coordinator note: it does — the LAB Cloud is `php -S` over the worktree and the appliance healed at tick 385 once 99b3afa landed.]
4. `job.preview_url` origin for Print Here / KOT fallback — built server-side, not traced (E-10/E-12).
5. `KotCancellationService` permission internals reached from Edge cancel (E-05).
6. Edge `printJobs()` terminal/branch scoping vs Online `PrintJobController:315-325` (E-05).
7. Installed compiled Blade cache (`INST/storage/framework/views`) vs source views — compiled output not compared; source views identical.
8. `vendor/` parity — only the manifest `vendor_lock_sha256` exists; not diffed.
9. Actual rendering of a 403/419 page on the appliance in a browser.
10. The parity register (`docs/status/edge-online-pos-parity-register.md`) uses FULL_OFFLINE_PARITY / ONLINE_REQUIRED / FINANCIAL_PARITY_PENDING, not this audit's STATUS vocabulary; mapping of "Cloud reports" (Sales Report Center button, E-02) to ACCEPTED_ONLINE_REQUIRED needs owner confirmation.
