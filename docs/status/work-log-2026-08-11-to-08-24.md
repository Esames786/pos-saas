# Bingoo POS SaaS — 15-Day Work Log (11–24 August 2026)

**Branch:** `feat/14d-2-plan-upgrade-requests` · **Production:** Hostinger `187.77.140.39`
**Commits in this window:** 239 · **Prod HEAD at close:** `a32d32d`
**Live tenants touched:** Khatri Biryani (restaurant, live since 11 Aug), Kashif Kitchen (catering), Kashif Food (restaurant, onboarded 24 Aug)

---

## What the fortnight actually produced

| Track | Outcome |
|---|---|
| **Khatri Biryani go-live** | First live trading day (11 Aug) survived and was hardened through ~60 fixes: printing, returns, discounts, shifts, timezone, reports |
| **Catering V1 (Kashif Kitchen)** | Built from nothing to a live vertical: events, quotations, cost blocks, materials, store issue, production releases, documents, finance |
| **Legacy migration** | The client's 20-year-old software database read directly and rebuilt into the system — 909 items with their own IDs, 18 categories, 4,848 customers |
| **Order Punch** | A keyboard-first order screen matching the old software's rhythm, with nothing saved until the operator saves |
| **Platform** | Permission-cache, tenant-context, entitlement boundaries, print-job numbering, print agent isolation |

---

## 11 August — Khatri's first live day, and its aftermath

The tenant went live and every rough edge surfaced at the counter. 40+ commits the same day.

**POS correctness**
- `dc0b870` harden POS submissions, data scope, print fallbacks
- `d8a28ef` refuse a sale line naming a product with no quantity
- `869c7c6` `35b08ac` dead POS buttons — Add & Attach, Save address, Complete Sale (two script blocks: shared helpers must be top-level)
- `1099553` final bill prints after a preview; a fresh order clears the customer
- `a91865f` `2d0f7fe` one-box customer flow; address choice always available on delivery
- `d0ac021` delivery orders require a customer
- `ca492f6` reassign a delivery rider, with an audit trail
- `4312f75` order-type scoped lists, live-reacting layout editor

**Money & returns**
- `e451202` `6fbe2ba` `c201f8f` sales returns made explicit; refund method mandatory
- `85ebcfd` `cedb073` return accounting fixed (carry + prorate discount/tax; double-counted tax on category values)
- `e49ba3b` full return gives the delivery charge back
- `bdfd1db` `719d2f6` manual discounts with per-branch approval
- `439cf22` split cancellation policy + manager codes
- `c66f254` Sales Summary was giving the counter the wrong cash target

**Printing**
- `9c471cd` KOT per category; bill preview = the real bill
- `f4758c9` receipt stopped starting a finger's width down the roll
- `847f4fa` `64eb58d` layout font size actually reaches the printer; preview gets the paper's size
- `bdc6fcd` `e1098ee` `e421721` print jobs no longer lost to a briefly-busy printer; printers kept awake; agent retries
- `0409a5c` `5a42035` version-stamped agent download (cached installer can't masquerade)
- `8993520` `c74ed6f` `d868a7d` `e6bcd9d` `7138700` thermal report: fits 72mm, reads as columns, bold enough to read, aligned to live printer profiles
- `0ec2fc7` `752daad` every report total reconciles to NET SALES; dashboard agrees with Report Centre
- `287f1d3` `15802c0` bill preview reads like the bill; seeder matches the live menu

**Data integrity**
- `98bcb73` duplicate branch — provisioner keyed the branch on its NAME
- `adae5e3` `e28aa75` combo/modifier/split-bill linkage guard; combo KOT routing
- `46a6fc6` sale times shown in the timezone they happened

---

## 12 August — tenancy, permissions, shifts

- `db9194f` **tenant identification now precedes the session** — closed the `/login` 500s (`No database selected`)
- `e17d6b5` **permission cache per-request (array store)** — ended the cross-tenant 403 storm
- `3348b98` tenant activation drops the previous tenant's permission collection
- `267d512` **dismiss obsolete print jobs without faking a print** (`markPrinted` = physical success only)
- `81bfd77` `37a8e9f` no close may invent a count; Daily Closing aggregates by business day
- `b795405` shift open/close limited to the cashier's own terminal
- `6192092` dine-in counter gets its own role, not Delivery's
- `eb61ce0` Print Jobs times read on the branch's clock
- `e4568c5` `014a6f2` column receipt/KOT/reminder, AM/PM, combos in cart; rounded money + report date filters
- `49694a8` bold what the rider reads at the door
- `04156b6` `889b44b` demo reset per-process, survives one failure
- `d6e52cd` SSL renewal runbook
- `6ce7357` chronicle of Khatri's first live trading day

---

## 13 August — Catering V1 begins, Edge fenced

- `0e85e02` **CATERING-V1 slices 1–3** — the Bingoo Catering & Events vertical (events, estimates, lifecycle)
- `0ddddda` costing readiness fails closed on send/confirm
- `5c747d5` agreed-event visibility, advance contract, final invoice + closure
- `3641900` production releases ride the PrintJob transport
- `a34231b` multi-tenant reminder proof, friendly permissions, platform non-regression
- `f67374b` demo closure + final-invoice blade fix
- `c4fc021` **EDGE-SPLITBRAIN-STOCK-1** — official stock authority fenced during Local Mode (dormant)
- `8d87db9` 500 on all four finance statement pages (Blade `@endforeach` compile bug)
- `c6b519e` network/reprint buttons printing the previous customer's bill
- `5e347df` env-driven Edge-local test DB isolation

---

## 14 August — go-live readiness, entitlement boundary

- `0e9c728` `855fbf7` `482e208` **CATERING-GO-LIVE-READINESS-1** — rollout gate (deploying code never broadens access), finance + inventory closure, demo acceptance
- `1e07356` canonical integration of `release/catering-go-live-2`
- `e473aca` dropped a route-less permission
- `0facf21` **403 on cached unnamed routes (root `/`)** + server-time poll exemption
- `67efbc1` **hold a delivery order and its charge comes back whole on recall** (a POS order field needs all 3 write-paths)
- `661cf0e` vehicle# on takeaway, delivery-charge recall, bigger cancel-KOT heading
- `73e6f7a` receipt preview: Qty/Rate/Amount stopped colliding
- `74cecec` self-signup/billing/payment/email research

---

## 15 August — catering told the truth about itself

- `2a4fb69` `cb7aa8e` **every catering action states its impact** (finance / stock / reversibility) — precise, not decorative
- `55949b2` say what each action costs; fix documents that printed wrong
- `addd008` Materials behave like a kitchen, not a factory
- `1e7b589` record WHY an event was cancelled
- `bc234bf` print choices on estimates and final invoices
- `37010c8` `498cd70` release-gate boundaries and document print authorization proven over HTTP
- `1da0bc4` **PLATFORM-ENTITLEMENT-BOUNDARY-1** — dashboard and sidebar entitlement corrected (permission ≠ entitlement)
- `3fb2ae9` a product list may live at a path other than `/products`
- `0a74301` froze the product-creation contract before touching anything near it
- `51367cc` Blade serialization defects + render coverage

---

## 16 August — money, credit, and the diary

- `a22151c` **customer credit and the booking financial position** — one settlement authority
- `00ef190` audited catering refunds; `9b4ad86` refunds require a mapped money-out account
- `e1c12ec` booking statement + settlement screens
- `a8a5f35` **price a dish from named cost blocks** (the grammar the whole vertical now runs on)
- `f9e259d` booking diary on the dashboard
- `ac09b61` one press = one record; yearly numbering; Kashif's plan as code
- `cbb480f` free the store from the order; stop showing POS to a caterer
- `cb40f03` six redirects that 500'd *after* doing their work

---

## 17 August — costing source, store issue

- `a8069a9` a dish chooses **recipe or cost-block** costing; `458a9ae` a switch requires a ready source
- `90083d9` costing dispatched per estimate line
- `08fa111` named cost blocks configured on catering products; `0d17f0b` explained in the storeman's language
- `6b81399` `acaec76` `f3b04fe` Store Issue: many bookings per issue, searchable materials, date+search selection
- `bdb44b7` tenant reset taught about catering documents
- `d724c79` dashboard, sales-order filter and shift screens scoped to the operator

---

## 18 August — Khatri print & routing overhaul (P1–P5)

- `468b9ef` **P1** dynamic item/time font, column dividers, KOT category toggle, preview↔receipt parity
- `acb1ee3` `0d41617` **P2** return-create scoping, default refund method, single-item return; takeaway drops vehicle, Quick Sale requires vehicle + waiter
- `9319a15` **P3 KOT routing keyed on the physical TERMINAL** (backward-compatible)
- `9f1a766` **P4** routing data switch (order-type → terminal) with a before/after safety check
- `703d789` **P5 REPORT-SEND-TO-NETWORK** — stream the Sales Report to a thermal printer via the agent
- `c39e335` Report Centre Print/Z/Export silently narrowing to one terminal
- `e3b2ed8` `6d81c7e` previews match the actual thermal print; Table Bill drops forced `.00`
- `5f3c259` `03ea564` guarded Cost-Block-first Kashif UAT dataset

---

## 19 August — cost snapshots, customer-supplied, print emphasis

- `5bb637d` **snapshot Cost Block pricing on estimate lines** (the line's own copy, never the master)
- `f0fa9ea` estimate-line Cost Details + quoted-rate override
- `46672ff` line costing decisions survive draft edits
- `d3b504b` material-unit commercial pricing basis
- `bfaf843` **customer-supplied estimate materials** (kitchen still needs it; our store hands over none)
- `1d66ab0` **one collision-safe authority for print job numbers**
- `786a414` `aae508a` `deedd7e` thermal highlights: rows a reader scans, actually bold, **emphasis by weight only** (enlarging broke columns)

---

## 20–21 August — reports reconcile, rates, finance

- `bec1d16` category/product filter honoured in order-level sections
- `78d6a6f` `8903742` order counts are **DISTINCT**, not per-product sums
- `dcd1ae4` **returns & item-voids reported by business day**, not calendar date
- `719b4bb` `24f6c29` **delivery-charge bridge** — every merchandise total closes to NET SALES
- `820e76c` a partial return could not post its remaining items (`disabled` never submits → `readonly`)
- `52b5c85` **supplier opening balance posts to the GL** (Dr 3300 Opening-Balance-Equity / Cr 2100 AP)
- `f7fe179` `f4fac92` `f2c689e` `c06f041` commercial material rates + rate impact; put a dish on the house rate; hardened; applied through quote revisions
- `1e2201c` `4749ffb` `231b222` rate impact and every draft mutation serialized with send
- `ad5dd32` `378868b` `df0ae9f` `c956de7` operations driven from quotation snapshots; store requirements reconciled without inventing allocations
- `d5e87e4` pricing and document state made explicit

---

## 22 August — the client's real menu, and their old screens

- `2d000fc` `73114ed` `9d55405` **operator completion** — draft workspace Phase B truth, managed instructions, bulk documents, calendar dashboard, event search
- `be7049d` `72b0c79` **Making charges classified explicitly** (`charge_role`) + preview/apply Making adjustments
- `3a92604` `b83706c` manual document resend; event create/edit stay inside the workspace (no reloads)
- `7502811` the client's **authoritative 55-label kitchen vocabulary**
- `0b5df5a` `5fc0f60` `8c6b4eb` **POS-DRAFT-1** — park a held sale as a DRAFT (KOT not sent), badge in recall
- `d945d35` **888-item real menu imported** without inventing commercial truth
- `acaf4c3` `f9907a7` Pakistan-market estimate pricing, labelled as estimates; spelling-corrected items still find their band
- `7ab5c96` an error response must never send the operator to the action URL (the MethodNotAllowed screen)
- `4ec46ea` `605028f` `91f7058` a block-costed dish shows its price the moment it is picked; full Cost Details instantly; outcomes as toasts, never a scroll
- `9cd89b5` legacy-screen habits honoured (open panels preserved, live quoted rate, Fare default, customer-supplied on documents)
- `c15da47` **the client's own 18 legacy categories** (sequence-hundreds proof), legacy strip on rows, bilingual materials on print
- `f4e7717` the old software's **ITEM screen** + **Karachi on every clock** (8 views were printing UTC)
- `ec09b6a` **Complimentary in one click**; print reads serial → item → materials → figures; POS-style sidebar hide

---

## 23 August — partial supply, history, and the punch

- `28500ad` a non-stock-tracked item cannot be a `stock_item` consumer
- `1914b85` **PARTIAL CUSTOMER SUPPLY** — one line, split supply: bills/buys/issues only the balance, kitchen keeps the whole
- `250ae48` **EVENT HISTORY + REVERT** — append-only revisions, restore a superseded version, revert to any checkpoint (money never touched)
- `3a730a0` **cost panel simplified** — linked "Customer dega / Hum denge" boxes, one rate in one place, booking-only rate change
- `36e94c7` **the client's own database staged and importable at will** (`extract-legacy-xlsx.php` → CSVs → `catering:import-legacy`)
- `193c012` per-item switches (**Allow Party Meat**, **Complimentry Item**) + visible keyboard shortcuts
- `0251b8f` **the guided punch, live** — item → Qty → Own/Party → materials one by one → Enter
- `f456e6b` `b54a327` a numeric query is a **CODE**, not a hash fragment; the dropdown speaks in item codes
- `fca8e4a` `1f3414a` `a131b12` the loop closes (save → item box reopens); rows land below; the punch IS the screen
- `609c7c2` **machine-UAT of the whole round trip** (bar → save → print → kitchen release) and an honest submit
- `8401e63` `508bf65` Send-to-Network scoped to the operator's own terminal
- `5bd3e29` `ae00c41` Report button in POS, opening in a same-screen window
- `804e7af` **POS-PRINT-HERE** — local/USB fallback when a network print fails
- `0896a39` print agent isolates printers so one offline unit stops delaying all prints

---

## 24 August — no auto-save, the rebuild, and a new tenant

**Order Punch finished**
- `eef5e4e` the sidebar obeys the operator (choice remembered)
- `c98181f` an uncaught JS error surfaces IN the punch bar, never as silence
- `82a6359` **the sweep that ate the order book** — a DOM "cleanup" was deleting saved rows on every swap
- `0538be4` **NOTHING SAVES UNTIL YOU SAVE IT** — Ctrl+Enter builds the row on screen; every setting rides along and reaches the server in ONE act
- `6e970b7` the sittings list (Sehri/Iftar/Mehndi/Walima…) is configuration — a reset keeps it
- `7273513` the **POS's customer flow** on the booking form
- `c259607` **PARTY→OWN kept the customer's share** (a hidden number that quietly changed money) + readable rows
- `8b8e75b` saved rows punch like new ones; the paper reads like paper (materials as one quiet line, no forced Urdu)
- `ffd8f7f` one grid language — and **the CSS that ate a column** (descendant selector reached into the nested breakdown table)

**Legacy rebuild (destructive, owner-approved, backed up)**
- `616f793` **KASHIF-LEGACY-REBUILD-1** — the catalogue rebuilt from the client's own database:
  - **SKU = legacy OrderItemId** — 909 items, 909 unique ids, duplicates impossible
  - **18 categories** by the item's own `OrderCatagaryId`
  - **Item 361 = 2,650 exactly** (Beef 1,450 + Making 1,200) — the legacy screen to the rupee, and `OrderRate = MeatRate + ServiceRate` holds on all 909 rows
  - 234 dishes get a **real material** at the house's own purchase rate; 96 carry the money as a charge that *says* the material is unnamed
  - Flags from the book: **445 party-allowed, 15 complimentary**, kitchens → stations
  - Units normalised (KGS/KG→KG, NOS/NO/PIS→PCS, P/H→PH…)
  - **4,848 customers**; GL and stock fingerprinted at zero movement

**New tenant + POS polish**
- `b3617c8` **KASHIF-FOOD-ONBOARD** — a brand-new restaurant tenant (4 terminals, 199 products, 41 combos, 3-station routing)
- `e9ebe23` recall must not silently switch the operator's active terminal
- `bb5964e` `f191680` hidden/service combo parts must not read as out-of-stock; a deal shows only its name
- `a82bbd6` `a32d32d` thermal report: stop the Qty/Amt shatter, short-form long names, darker figures, drop the `.00`

---

## Standing guarantees held throughout

- **Khatri Biryani was never disturbed** by catering or Kashif work — read-only smoke after every deploy (`tb_diff = 0`)
- **No deploy without green gates** — the catering suite finished the fortnight at **584/584**
- **Every destructive action was backed up first** (the 24 Aug rebuild: `backups/20260823_220315`, Kashif dump 1.6 MB)
- **GL and stock fingerprinted** before/after every import and rebuild — the commands refuse to report success if either moved

## Open items carried forward

- Purchase → Material Rate Book sync (client's request, needs its own design)
- Legacy Phase-3 fields: per-item Urdu name, Qty-In-No
- Edge / offline track remains frozen and dormant (parallel session active on `feat/edge-config-refresh-v1`)
- Wildcard certificate `*.bingoopos.com` expires **17 Sep 2026** — renewal is manual DNS-01 today
