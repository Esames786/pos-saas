# Offline Edge — FULL cashier parity implementation: execution board

Owner directive 20 Sep 2026 (after the 149-record audit, commit b678568). Baseline = master report
`docs/status/edge-online-vs-edge-screen-audit-2026-09-20.md` + appendices `docs/status/audit-2026-09-20/`. The Online POS is the
UI **and** functional reference. No re-audit. Every record is backlog. Owner-dependent items (§7 of the directive) are isolated
below and are NOT started.

Standing safety state: `YELLOW_CABLE=connected · LOCAL_MODE=inactive · AUTO_FAILOVER_ENABLED=no · P6_STARTED=no · LIVE_TENANT_MUTATIONS=none ·
LAB_APPLIANCE=unchanged (0.6.0-edge, no reinstall/reset/re-pair)`.

## Board (20 Sep 2026)

| WORKSTREAM | OWNER/AGENT | FILES_OWNED | DEPENDENCIES | IMPLEMENTATION_STATUS | TEST_STATUS | BROWSER_PROOF_STATUS | BLOCKER |
|---|---|---|---|---|---|---|---|
| **W0 page decomposition + control census** | coordinator (this session) | `resources/views/edge/pos/{index,partials/*,js/*}.blade.php`, `EdgeLocalPosController@screen` (vm), `tests/Fixtures/edge/online-pos-control-census.json`, `tests/MySql/EdgeCashierControlCensusHttpMySqlTest.php` | — | **DONE** — commit f7e977a. 19 fragments, one owning team each; runtime unchanged (same IIFE); stable ids on action buttons + header context | **GREEN** — census 4/4 (278 Online controls: present 11 / equivalent 113 / partial 36 / planned 101 / online_required 17), render 3/3, node --check of the composed script OK, route census (Feature) 4/4 unchanged | not applicable (no visual change) — harness ready (see W7) | none |
| **W0b safety-critical gates** (independent, owner-free) | coordinator | `EdgeLocalPosController` (gates + terminal authority + hidden-amounts), `EdgeLocalReturnService::search` (data scope), `tests/MySql/EdgeCashierRouteGatesHttpMySqlTest.php`, `tests/MySql/Support/EdgeLocalRuntimeFixture.php` (Online cashier permission set), `EdgeCashierPermissionHttpMySqlTest` | W0 | **DONE** — Online route permissions enforced server-side: `tenant.pos.index`, `held-sales.store/cancel`, `split-bill.store`, `table-sessions.open/close`, `shifts.store/close`, `api.manager-approvals.verify`; terminal pin + terminal assignments enforced on select AND on every use; `GET /shift` strips amounts by the shared AmountVisibility rule; returns search + Recent Prints follow UserDataScope | **GREEN** — gates 4/4 (77 assertions), permission 1/1; broad Edge regression 98 tests / 1171 assertions: 1 failure = stale in-memory permission relation in the shared fixture (not a product defect), fixed, suite re-run green | pending (LAB update is owner-gated); dev Edge instance being prepared for the harness | LAB appliance web backends are DOWN (see LAB note) |
| **W0c controller split by team** | coordinator | `EdgeLocalPosController` (page/catalogue/terminal/sale/customers/sync — Team 2), `EdgeLocalShiftController` (Team 4), `EdgeLocalRestaurantController` + `EdgeLocalHeldSalesController` (Team 3), `EdgeLocalManagerApprovalController` + `EdgeLocalReturnController` (Team 4), `EdgeLocalPrintJobController` (Team 5), shared `Concerns/ResolvesEdgePosContext` (terminal + gates), `routes/edge_runtime.php` re-pointed (same URIs/names) | W0b | **DONE** — 1 143-line controller → 7 controllers + 1 trait, behaviour-identical; enables "one owner per controller" | Feature route census + artifact boundary 17/17 GREEN; MySQL regression → see below | dev instance re-verified (health 200) | none |
| **W7 dev Edge instance** | coordinator | `tests/MySql/EdgeDevInstanceSeedMySqlTest.php`, `tools/edge-dev-instance/*`, `.env.edgedev.example` | W0 | **DONE** — commit ac46a59; seeded production-shaped menu (14 products, 4 variants w/ barcodes, 2 modifier groups, 2 deals, 10 tables, 3 waiters, void reasons, delivery, customers+addresses, network printers → FakePrinter loopback, cashier DEVCASH1 pinned + blind count, manager DEVMGR1) served at 127.0.0.1:8095 | seed 1/1 | **FIRST BROWSER PROOF DONE** — Microsoft Edge headless: login, 3 viewports, 7 dialogs; DOM census present 11/11, equivalent 40/113 seen (29 deeper-flow, 44 text/js selectors), partial 16/36 seen; evidence archived `C:\Users\Dell\BingooEdgeLab\evidence\dev-instance\2026-09-20T09-08-22-943Z\` | none |
| **Teams spawned 20 Sep ~14:55 PKT** | 6 background agents (Teams 1–6) | per `docs/status/edge-parity-team-charter.md` §1; per-team test DBs `pos_test_*_edgewt_tN`; reports land in `docs/status/edge-w{N}-team{N}-report.md` | commits up to 63b88d8 (pushed) | **ABORTED before any edit** — all six agents were terminated by the model rate limit within their first minute (no file touched, verified by `git status`); re-spawned 24 Sep 2026 on a different model tier | — | — | rate limit (resolved by re-spawn) |
| **W1 shell / navigation / states / responsive** | Team 1 (running) | `edge/pos/partials/{styles,header,banners,shell}`, `js/{core,sync,boot}`, view-model flags | W0 | NOT STARTED | — | — | none |
| **W2 real menu + sale workflows** | Team 2 | `partials/{grid,cart}`, `js/{catalog,cart,commercial,payment}`, `EdgeLocalPosService` line resolution/validation, `EdgeLocalPosController@sale/preview` | W0; W6 for envelope fields (modifiers, tips, line discounts, notes) | NOT STARTED | — | — | owner decision for non-cash tenders (isolated) |
| **W3 hand-back (25 Sep)** | Team 3 (done) | `js/{held,tables,actions}`, restaurant + held-sales controllers, new `EdgeLocalTableOperationsService`, `EdgeLocalOrderLifecycleService`, 8 routes (W3 blocks in routes / registration test / `config/edge.php` route_allowlist), tests `EdgeCashierTablesWorkspaceHttpMySqlTest` (7), `EdgeCashierOrderLifecycleHttpMySqlTest` (4); report `docs/status/edge-w3-team3-report.md` | W0 | **DELIVERED** — table workspace with Online ids (board/open/held/move/split/merge/detail/reserve-with-customer/manage hint), session bar, recalled bar, Held Orders + Recent Orders with filters, Change Order (table picker), dead-session recovery, Clear / New Order / New Sale, sent-line void with reason + grouped `void_kot_items` approval for deals, cancel-with-approval; R14 partial (print block awaits reconcile), R21 partial (order-type change awaits Team 2), R19 blocked (bootstrap copies table status only → Team 6), R26 blocked (reconcile) | 31 tests / 711 assertions green; Feature 17/17; census row gate: 78 W3 flips applied by the coordinator (validated by the gate — no W3 row fails) | read-only dev proof by the team; mutation proof = coordinator | none |
| **W4 hand-back (25 Sep)** | Team 4 (done) | `js/{shift,returns,reports}`, shift/return/approval/quick-report/finance controllers + services, finance Blade pages (shifts, sales returns, supplier payments, journals, purchase returns list/detail), `config/edge.php` capabilities + W4 route_allowlist block, 12 routes, 5 new test classes; report `docs/status/edge-w4-team4-report.md` | W0b | **DELIVERED** — open shift (required opening cash + notes), close shift with denomination grid / live short-over / closing notes / post-close summary (blind-count leak in the close response fixed), shift history + detail screens, returns screen with Online ids + confirm + UserDataScope on screen and posting, returns list/detail, manager-approval payload validation (R4.1), Quick Report filters (waiters/order types/items with chips) + local saved selection + auto-print + business-name header, supplier payments / journals / purchase-return lists (reverse + drafts documented as owner-dependent), every Team 4 endpoint permission-checked; owner items O-1..O-5 and contract items C-1..C-4 documented | 27 W4 tests (1 needle fixed, re-run green) + 28-test regression green; Feature 25/25; census: 16 flips applied by the coordinator | not run by the team; ids listed per record | none |
| **W5 hand-back (25 Sep)** | Team 5 (done) | `js/printing`, `EdgeLocalPrintJobController` (rewritten), new `EdgeLocalPrintKotService` / `EdgeLocalPrintDirectPayService` / `EdgeLocalPrintDocumentService`, `EdgeLocalPrintDeliveryService` (dismiss + retry of dismissed), 7 routes (W5 blocks incl. route_allowlist), `EdgeCashierPrintingParityHttpMySqlTest` (8); report `docs/status/edge-w5-team5-report.md` | W0 | **DELIVERED** — proven over HTTP with real fake printers: 3-station routing from another counter (D-01), ADDITION deltas (D-02), DUPLICATE copies (D-03), KOT REMINDER auto/ask/confirm/decline/reprint (D-04), cancel KOT + cancellation reminder at the acting counter (D-05), Direct-Pay KOT+receipt once/idempotent/retry (D-08), receipt reprint (D-09), canonical bill preview without mutation (D-10), retry + dismiss with live-print guard (D-13/14), data scope (D-23), terminal auto-print prefs (D-24); waits on Team 2 (T2-1 print prefs in Review & Pay + intents on POST /sales, T2-2 terminal to recordLineCancellations/queueKot) and Team 3 (T3-1..5 wiring: fireKot, cancel jobs → handlePrintJobs, void correction reminders, openLastPrint, table bill print) for the screen paths; behaviour note: no automatic receipt without a terminal setting/toggle, like Online; D-16/17 owner ruling, D-18/21 Online-only | 48 tests / 762 assertions green; Feature 17/17; Cloud printing Feature 9/9; 0-line diff in shared printing services | not run by the team; ids listed per record | none |
| **W1 hand-back (25 Sep)** | Team 1 (done) | `partials/{styles,header,banners,shell}`, `js/{core,context,sync,boot}`, `edge/auth/login.blade.php`, NEW `EdgeLocalAssetController` + `GET /edge/local/assets/{path}` (whitelisted, unauthenticated, ETag/304, no session), `EdgeCashierShellHttpMySqlTest` (9), `EdgeLocalAssetRouteTest`; report `docs/status/edge-w1-team1-report.md` | W0 | **DELIVERED** — Online light shell (Bootstrap 5.3.8 + Tabler icons + SweetAlert2 through the local asset route, inline fallback when the route is off), 500px cart, 148px tiles / 44px tabs / 42px buttons, Online breakpoints (≥1200 / ≤1199 / ≤991 / ≤800), POS header row (sidebar toggle, title, View Tables, session bar markup, order-type TABS with hidden `#order-type`, customer slot with Return / Quick Report / Add-Search Customer, context row branch·terminal + Change + shift badge polling GET /shift + no-terminal warning, order tools Held Orders / Recent Orders / Prints; Edge-only links in a collapsible strip), Branch & Terminal dialog (branch fixed), busy buttons / loading bar / severity toasts / confirmDialog / 401-419 → login, calculator, Ctrl+F/H/L/P/Enter/M + Esc, deep links (`?customer_id=` awaits Team 2 exact-id lookup), inline icon (no favicon 404), login page restyled; A35 pills 40px vs 44px awaits Team 2 | 22 tests / 332 assertions green; Feature 23/23; census: 26 flips applied by the coordinator | read-only dev proof by the team (no horizontal scroll at 3 viewports; measured sizes) | none |
| **W3 follow-up (25 Sep)** | Team 3 (done) | same files | Team 5 / Team 1 | **DELIVERED** — sendKot → `fireKot` (Reminder plan), auto-KOT after Hold/Save round when `autoPrintEnabled('kot')` else "Print Kitchen Order?" like Online, cancel returns `jobs` → `handlePrintJobs`, void correction reminders after a revise, Recent Orders → `openLastPrint`, table bill → `printBillPreview` table payload; session bar fills the header ids, `#edit-order-btn` in the recalled bar, light-theme styles; decision taken: Review & Pay keeps Online's hold → KOT → pay order (prompt at pay if food is unsent) | 31 tests / 728 assertions green; node --check green | — | none |
| **W2 hand-back (25 Sep)** | Team 2 (done) | `partials/{grid,cart}`, `js/{catalog,cart,commercial,payment}`, `EdgeLocalPosController`, `EdgeLocalPosService`, `EdgeCashierMenuHttpMySqlTest` (8), `EdgeCashierPaymentHttpMySqlTest` (4); report `docs/status/edge-w2-team2-report.md` | W0 | **DELIVERED** — parent/child category strips (duplicate Deals pill fixed), tiles with sku/stock/unavailable/backorder/combo badges, barcode + SKU scan (product + variant barcodes exported), measurable qty modal, variant picker, options modal priced server-side with min/max + linked-product stock (needs bootstrap v7 on a real appliance), cart direct qty / remove / options edit / sent-line Cancel, line discounts (paid line-only-discount sale refused until the envelope guard lifts), held-sale notes persisted, customer modal (search/attach/addresses; quick-create hint), delivery panel on the main pane with Online rider rules, manual discount with approval-on-Apply + short-tender shortcut, promo feedback, tips quoted (paid tip refused until the guard lifts), live server totals, Online payment modal (full method list, non-cash disabled — card Cloud-only hint, bank/cheque/other awaiting owner), print intents + Team 5 wiring, inline quick-sale fields, direct Dine-In sale → Online's "Select an open table…" message; kitchen notes: Online captures none → no till control (server accepts; owner decision) | 86-test team regression (1 stale assertion fixed, 17/17 re-run); Feature 17/17; census: 75 flips applied | ids per record; coordinator proofs below | none |
| **Integration state (25 Sep, coordinator)** | coordinator | per-team commits: 9b2692d W6, efbd239 W1, 2c7ef88 W2, 91a9b23 W4 (+ shared `CashCountService`), 845bb02 W5, 068398c W3; shared surfaces (routes, registration test, `config/edge.php`, census fixture/test, Blade gate, compatibility classifier, login landing, harness, board) in the final integration commit | all teams | **CENSUS: 278 Online controls = present 222 / equivalent 35 / partial 4 / online_required 17 / planned 0** (partial: paymentModal + billPreviewModal/body + quickReportModal text anchors; R14 print block awaits reconcile). Feature Edge directory 136/136 green (Blade gate fixed for the fragment shell); Online + Edge shift suites 18/18; compat 8/8 | full Edge MySQL regression running (log `regress-integrated.log`) | read-only integrated proof: 0 console errors, 0 refusals, 3 viewports without horizontal overflow, 9 dialogs opened; **mutation proof (dev instance, Microsoft Edge): 202 of 222 present rows seen in the live DOM** — shift opened, quick-sale fields, qty + options modals, manager prompt at Apply, customer search/attach, delivery panel, table opened (G4) → held check → auto-KOT (ONE batch, 3 routed station jobs — verified in the dev DB) → split / cancel / change-order dialogs; unseen: print flows (need a paid sale), dead-session, reservation details (reserve refused on the dev instance: "not under Branch Server authority" — expected without a lease; LAB run covers it); evidence `C:\Users\Dell\BingooEdgeLab\evidence\dev-instance\integrated\` | none |
| **Coordinator follow-ups (25 Sep)** | coordinator | `EdgeLocalAuthController` (cashier lands on the POS after login, status page otherwise; clean-machine test accepts both), js/printing in-dialog `#last-print-btn` → `#last-print-open-btn` (duplicate id, Team 1 finding), finance layout inline icon, browser-proof tool re-pointed to the Online ids with pre-parity fallbacks (mode tabs, `#product-grid`, `#pos-return-btn`, confirm dialogs) | `app/Services/Sales/CashCountService.php` (NEW shared rule; `ShiftController@calculateCashCount` and `EdgeLocalShiftController` delegate to it), `EdgeCompatibilityService::CLOUD_OFFLINE_FEATURES` (+ the five W4 capability names; broad `returns`/`purchasing` stay unavailable offline), census flips applied for Teams 3 (78) and 4 (16), Team 5 pending | Teams 3/4/5 | IN PROGRESS | compat + shift suites running | — | none |
| **W3 tables + order lifecycle (planning row)** | Team 3 | `js/{held,tables}`, restaurant/held controllers + services, new `edge.local.pos.*` routes (route census must be extended deliberately) | W0; W6 only for the combo-void reconcile | NOT STARTED | — | — | none |
| **W4 shifts / permissions / finance** | Team 4 | `js/{shift,returns,reports}`, shift/returns/quick-report controllers, finance views, `edge-build-manifest.json` capabilities | W0, W0b (done); W6 for new events | NOT STARTED (W0b covered the verified permission gaps + hidden-amounts leak) | — | — | owner decisions: Close Branch / Daily Closing, journal reversal, PR drafts (isolated) |
| **W5 kitchen printing** | Team 5 | `js/printing`, print-jobs controller, print worker, KOT/reminder services | W0 | NOT STARTED | — | — | none |
| **W6 wave 1 — design + assessment (24 Sep)** | Team 6 (done) | `docs/status/edge-w6-contract-and-reconcile-plan.md` (design only, no code) | — | **DELIVERED** — reconcile risk LOW (canonical b529c95, 59 commits past 5dc13d3, merge-tree 0 conflicts; appliance-executed changes = KotCancellationService combo-void fix, receipt.blade dormant @isset block, one additive tenant migration); packaging gap: Catering job/support files would ship → add excludes; contract verdicts: modifiers ids/names/deltas additive on envelope v1, modifier linked-product stock = envelope v2 (non-additive), tips + line discounts additive (builder guards block them today), held-sale notes additive (never persisted, not in envelope), kitchen notes BLOCKED pending re-check of the audit A10 claim (canonical writes no kitchen_note), shift close to Cloud = new event (owner-dependent); local-only: request-bill/move/merge/reattach, print intents, denomination count, quick-report settings; **bootstrap export must move to v7** (no product_modifier_group rows / global modifier groups today) and must land immediately before the LAB update; outbox `SCHEMA_UNSUPPORTED` → retryable needs coordinator approval (touches the outbox engine); release gates 1–10 listed | — | — | none |
| **W6 Cloud/Edge contract + reconcile + release (code)** | Team 6 wave 2 (not started) | `EdgeSaleEnvelopeBuilder`, Cloud `EdgeInboundSaleIngestionService`, canonical reconcile (243e01d incl. MANAGER-APPROVAL-COMBO-VOID-1), release build | — | NOT STARTED | — | — | none (starts with the reconcile assessment) |
| **W7 continuous verification** | Team 7 / coordinator | `tests/MySql/EdgeCashier*`, census fixture, `tools/edge-browser-proof/` (Playwright on the installed Edge/Chrome, no download) | every lane | **HARNESS READY** — `tools/edge-browser-proof/edge-pos-proof.mjs`: login, DOM census per dialog, paired-viewport screenshots, report with denominators; Playwright package install in progress | census gate is the regression gate | needs a target: dev Edge instance (next) or the LAB after the owner-approved update | no dev Edge instance yet; LAB backends down |

## Regression (W0 + W0b)

Filter: `EdgeCashier* | EdgeLocalPosHttp | EdgeLocalRestaurantHttp | EdgePurchaseReturnHttp | EdgeSupplierFinanceHttp | EdgeLocalPosMySqlTest | EdgeLocalPosRaceTest | EdgeReturnSyncHttp`
— 98 tests, 1171 assertions, 3m08s. One failure (`EdgeCashierReturnHttpMySqlTest` 403 on returns search): the test granted the
return permission AFTER the new gates had already lazily loaded the acting user's permission relation; a real request re-resolves
the user, so the shared fixture now drops the stale relation on grant. Re-run of that suite: 3/3 green. Route census (Feature) 4/4.

## LAB note (read-only observations, 20 Sep 2026)

~13:30 PKT: two curl probes to the LAB gateway (8443) and to the web backends (8090/8091) returned nothing within 25 s, which I
first read as "backends down". Re-probed at ~14:35 PKT: gateway `https://127.0.0.1:8443/edge/local/health` → 200 in 0.7 s, both
backends 8090/8091 → 200 (`edge_app_version 0.6.0-edge`), and the supervised processes (serve workers 1–2, authority worker, print
worker, nginx) have been running since 00:02 PKT. So the appliance is UP and unchanged; the earlier timeouts were transient (the
probe ran while the appliance was busy) — **no restart is needed and none was performed**. FakePrinter (9100) and LAB Cloud (9701) up.
(PowerShell 5.1's Invoke-WebRequest cannot complete the TLS handshake with the LAB CA/TLS 1.2 gateway — a client-side limitation,
not an appliance fault; curl and the browsers work.)

## Owner-dependent items (isolated; NOT started, NOT excluded)

| Item | Records | Needs |
|---|---|---|
| Offline bank-transfer / cheque / other tenders and refunds | A21, R2.1, R3.4 | envelope + ingestion contract (W6), owner approval |
| Close Branch / Daily Closing financial posting offline | R1.10 | Cloud posting contract, owner approval |
| Manual journal reversal offline | R8.6 | new finance event, owner approval |
| Purchase-return drafts offline | R9.2 | draft lifecycle event, owner approval |
| Customer / printer administration on Edge | R7.2, D-16, D-17, A13 | owner ruling (admin vs operator) |
| Cloud-only reporting scope (Reports Center) | A44, R5.6 | unchanged by this directive |

## Pre-update requirements found during integration (25 Sep)

- **LAB cashier permissions.** The appliance now gates every POS action by the Online route permission (W0b), including
  `tenant.pos.index` for the page itself. The LAB Cloud seed grants LAB2C5D only `tenant.pos.store / view / hold / recall`
  (`C:\Users\Dell\BingooEdgeLab\scripts\lab-cloud.php`), so after the 0.7.0-edge update the LAB cashier could not open the POS until the
  LAB Cloud grants the Online cashier set (`onlinePosParityPermissions()` in the test fixture lists it) and a config refresh syncs it.
  Release gate: grant on the LAB Cloud BEFORE the update; the same check applies to every real cashier role (an Online cashier already
  holds these — verify per tenant with the bootstrap export).
- **Bootstrap v7 + Catering packaging excludes** (Team 6) must land in the same release; the LAB Cloud runs from this worktree, so v7
  breaks config refresh on the running 0.6.0 appliance the moment it lands — sequence it immediately before the owner-approved update.
- **Test isolation fixes made today (test-only):** `EdgeCleanMachineInstallMySqlTest` cashier now holds the Online cashier set;
  `EdgeBackupRecoveryAuthorityMySqlTest` cleans the master test DB by branch as well as tenant (30 stale keys since 14 Sep).

## Acceptance (second Windows cashier laptop) — not yet reached

Before the LAB update: changed files + release commit, restricted-artifact audit, signed-update verification, migrations/rollback,
LAB outbox/backup state, any Administrator-needing step, exact update/rollback procedure → **owner approval**. After: every
in-scope screen/workflow from the second laptop, paired Online/Edge screenshots at the same viewport, browser-executable
evidence (`tools/edge-browser-proof`). No live-tenant transactions. No P6.
