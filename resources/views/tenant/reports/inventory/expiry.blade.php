@extends('layouts.app')
@section('title', 'Expiry Report')
@section('content')
<div class="page-wrapper"><div class="content"><div class="page-header"><div class="page-title"><h4>Expiry Alerts</h4><h6>Batches expiring soon</h6></div></div>
<div class="card border-0 shadow-sm mb-3"><div class="card-body py-2"><form method="GET" class="row g-2 align-items-end"><div class="col-md-2"><input type="date" name="date_to" class="form-control form-control-sm" value="{{ request('date_to', today()->addDays(30)->format('Y-m-d')) }}"></div><div class="col-md-2"><select name="branch_id" class="form-select form-select-sm"><option value="">All Branches</option>@foreach($branches as $b)<option value="{{ $b->id }}" {{ request('branch_id') == $b->id ? 'selected' : '' }}>{{ $b->name }}</option>@endforeach</select></div><div class="col-md-auto"><button type="submit" class="btn btn-primary btn-sm">Apply</button></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="card-body p-0"><table class="table table-sm mb-0"><caption class="visually-hidden">Expiry batches</caption><thead class="table-light"><tr><th scope="col">Product</th><th scope="col">Variant</th><th scope="col">Branch</th><th scope="col">Batch</th><th scope="col" class="text-end">Qty</th><th scope="col">Expiry Date</th><th scope="col" class="text-end">Days</th></tr></thead><tbody>@forelse($batches as $b)@php $days = $b->expiry_date->diffInDays(today()); $urgent = $days <= 7; @endphp<tr class="{{ $urgent ? 'table-danger' : '' }}"><td>{{ $b->product?->name }}</td><td class="text-muted small">{{ $b->variant?->name ?? '—' }}</td><td>{{ $b->branch?->name }}</td><td><code class="small">{{ $b->batch_no ?? '—' }}</code></td><td class="text-end">{{ number_format($b->quantity_on_hand, 3) }}</td><td>{{ $b->expiry_date->format('d M Y') }}</td><td class="text-end @if($urgent) fw-bold text-danger @endif">{{ max(0, $days) }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No items expiring soon.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $batches->links() }}</div></div></div></div>
@endsection
